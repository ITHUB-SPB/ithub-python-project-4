# Проект «noLXP»

## Описание

Проект представляет собой срез части функционала lxp-платформы.

**Со стороны студента** это следующие клиентские страницы:

- страница с перечнем курсов и содержимым каждого из них,
- страница с детальной информацией по курсу, перечнем учебных тем и контрольных точек,
- страница учебной темы с обучающей информацией,
- страница контрольной точки с заданием, блоком отправки ответа и результатами,
- страница логина;

**Со стороны преподавателя** это следующий функционал в панели администрирования:

- логин в панель администрирования,
- просмотр содержания курса (учебных тем),
- просмотр контрольных точек курса,
- просмотр ответов на контрольные точки и выставление оценок и статусов;

**Со стороны менеджера учебного процесса** это следующий функционал в панели администрирования:

- логин в панель администрирования,
- администрирование аккаунтов студентов и преподавателей,
- администрирование содержания дисциплины (учебных тем),
- администрирование контрольных точек дисциплины,
- прикрепление (и открепление) преподавателей к курсам,
- просмотр курсов, учебных тем и контрольных точек, 
- просмотр ответов студентов со статусом и оценкой преподавателя,
- выгрузка (экспорт) в табличный вид.

---

## Требования и критерии (Часть I)

### Моделирование предметной области (5 баллов)

#### Студенты и учебные группы (`students/models.py`)

*до 2 баллов*

Модель учебной группы `Group` должна содержать:
- ✅ название (обязательное непустое строковое поле, длина <= 15);
- ✅ курс (обязательное непустое строковое поле), допустимые значения предзаданы (через `choices`);

Модель студента `Student` должна содержать:
- ✅ отдельные поля для имени (обязательное непустое строковое поле, длина <= 20), фамилии (обязательное непустое строковое поле, длина <= 40) и отчества (необязательное строковое поле, длина <= 40);
- ✅ поле `account`, отношение 1-to-1 с аккаунтом (моделью `User`), настроено каскадное удаление;
- ✅ `group`, отношение m-to-1 с учебной группой (моделью `Group`) через внешний ключ, при удалении связанной 
  группы должно выставляться значение `NULL`;

Метаданные:
- поля моделей переведены через `verbose_name`;
- группы сортируются по возрастанию номера курса;
- студенты сортируются по фамилии и имени;
- добавлены индексы по курсам;

#### Дисциплины и курсы (`courses/models.py`)

*до 2 баллов*

*Предварительно создайте приложение `courses`, подключите его к настройкам*

Модель `Discipline` должна содержать:
- поле `title` с названием дисциплины (обязательное непустое строковое поле, длина <= 50);
- поле `duration` с длительностью дисциплины (обязательное целочисленное поле, значение >= 0); 
- поле `curriculum` для ссылки на учебный план дисциплины (необязательное файловое поле);
- поле `created_at` с датой и временем создания (авто-добавление);
- поле `updated_at` с датой и временем последнего обновления (авто-обновление);

Модель `Course` должна содержать:
- поле `code` с кодом курса (обязательное непустое строковое поле, длина <= 15);
- поле `about` с описанием курса (необязательное строковое поле, длина <= 200);
- поле `course_start` с датой начала курса (необязательное поле);
- поле `course_end` с датой завершения курса (необязательное поле);
- отношение m-to-1 с дисциплиной (моделью `Discipline`) через внешний ключ, при удалении связанной группы должно выставляться значение `NULL`;
- отношение m-to-1 с учебной группой (моделью `Group`) через внешний ключ, при удалении связанной группы использовать каскадное удаление;
- отношение m-to-1 с преподавателем (моделью `Teacher`) через внешний ключ, при удалении преподавателя должно выставляться значение `NULL`

Метаданные:
- поля моделей переведены через `verbose_name`;
- дисциплины сортируются по датавремени последнего обновления, далее по датевремени создания;
- курсы сортируются по кодам связанных дисциплин;
- добавлены индексы на те поля, где это способно улучшить производительность;

#### Сотрудники (`staff/models.py`)

*Предварительно создайте приложение `staff`, подключите его к настройкам*

*до 1 балла*

Модели преподавателя `Teacher` и менеджера учебной части `Manager` должны содержать:
- отдельные поля для имени (обязательное непустое строковое поле, длина <= 20), фамилии (обязательное непустое строковое поле, длина <= 40) и отчества (необязательное строковое поле, длина <= 40);
- поле `account`, отношение 1-to-1 с аккаунтом (моделью `User`), настроено каскадное удаление;

Метаданные:
- поля моделей переведены через `verbose_name`;
- модели сортируются (в порядке убывания весов) по фамилии, имени, отчеству;

### Часть 1. Наполнение данными (4 балла)

Реализуйте наполнение дамми-данными через фикстуры и метод `manage.py loaddata`:

- добавить 3 аккаунта и связанных с ними студентов в `fixtures/students` (1 балл);
- добавить 2 аккаунта преподавателей и 1 аккаунт менеджера в 
  `fixtures/staff` (1 балл);
- добавить 3 дисциплины и по два курса связанных с каждой из них курса в 
  `fixtures/courses` (2 балла);

### Часть 1. Функционал студента (6 баллов)

*Предварительно добавьте директории с шаблонами для соответствующих приложений в `DIRS` объекта 
`TEMPLATES` в настройках проекта*

*При реализации обращайте внимание на имеющиеся в директории `assets` скриншоты. Повторять визуал не требуется, но важно чтобы присутствовали все данные (помимо тех, чья реализация в этой части задания не требовалась)*

По адресу `/auth/login` отрабатывает вьюшка логина для ввода имени и пароля. Ошибки логина 
передаваются через `messages` и отрисовываются над формой. При успешном логине происходит перенаправление. 
(2 балла)

По адресу `/auth/logout` отрабатывает вьюшка логаута с перенаправлением на страницу логина. (1 балл)

По адресу `/courses` отрабатывает вьюшка списка курсов. Отображаются только те курсы, которые относятся к 
группе залогиненного студента. При отсутствии аутентификационных данных пользователь перенаправляется на 
страницу логина (2 балла).

По адресу `/courses/:id` должна отрабатывать вьюшка конкретного курса. При отсутствии аутентификационных 
данных пользователь перенаправляется на страницу логина (1 балл).

---

### Часть 2. Моделирование предметной области (5 баллов)

#### Учебные темы (`courses/models.py`)

*до 1 балла*

Модель `Topic` должна содержать:
- поле `title` с названием темы (обязательное непустое строковое поле, длина <= 50);
- поле `content` для содержимого (обязательное непустое строковое поле без ограничений);
- поле `duration` для количества часов, отведённых на тему (обязательное натуральное численное  поле);
- поле `discipline`, отношение m-to-1 с дисциплиной (моделью `Discipline`), при удалении связанного поля выставляется в `NULL`.

#### Контрольные точки и оценки (`assignments/models.py`)

*до 2 баллов*

Модель `Assignment` должна содержать:
- поле `title` с названием темы (обязательное непустое строковое поле, длина <= 50);
- поле `content` (обязательное непустое строковое поле без ограничений);
- поле `discipline`, отношение m-to-1 с дисциплиной (моделью `Discipline`), при удалении связанного поля 
  выставляется в `NULL`;
- поле `weight` с максимальным количеством баллов;

### Часть 2. Наполнение данными (6 баллов)

### Часть 2. Функционал студента (10 баллов)

### Часть 2. Функционал преподавателя и менеджера (5 баллов)

https://testdriven.io/blog/dockerizing-django-with-postgres-gunicorn-and-nginx/#postgres
